<nav class="navbar navbar-expand-lg bg-dark border-bottom border-body" data-bs-theme="dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Restaurant</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <%= link_to 'Рестораны', caves_path, class: "nav-item nav-link"%>
          <%= link_to 'Создать', new_cafe_path, class: "nav-item nav-link"%>
          <%= link_to 'Мои кафе', my_caves_path, class: "nav-item nav-link"%>
        </ul>
        <form class="d-flex" role="search">
          <% if current_user.present? %>
            <%= link_to 'Выход', destroy_user_session_path, class: "btn btn-outline-light me-2", data: { "turbo-method": :delete } %>
          <% else %>
            <%= link_to 'Вход', new_user_session_path, class: "btn btn-outline-light me-2" %>
            <%= link_to 'Регистрация', new_user_registration_path, class: "btn btn-outline-light me-2" %>
          <% end %>
        </form>
      </div>
    </div>
  </nav>
